ext.baseVer = "0.0"
apply from: "../common.gradle"

group 'blue.sparse.minecraft'

repositories {
    flatDir { dirs "plugins" }
}

dependencies {
    compile project(':core')
    file("plugins").listFiles().each {
        compileOnly name: it.name.replace(".jar", "")
    }
}